@model Votor.Areas.Portal.Controllers.OptionModel

@using (Html.BeginForm("AddOption", "Event", FormMethod.Post))
{
    <div class="input-field">
        <i class="material-icons prefix">format_list_bulleted</i>

        @Html.HiddenFor(model => model.Id)
        @Html.HiddenFor(model => model.EventId)

        @Html.LabelFor(model => model.Option)
        @Html.TextBoxFor(model => model.Option)
        @Html.ValidationMessageFor(model => model.Option, "", new
        {
            @class = "red-text text-darken-1"
        })
    </div>

    <button type="submit" class="btn waves-effect waves-light @ViewBag.mainColor">@Localizer["Add"]</button>
}